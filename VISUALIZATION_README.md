# Визуализация результатов анализа статей

## Что хранят JSON файлы?

### 📋 Чек-листы (`checklists/*.json`)
Определения стандартных чек-листов для оценки качества научных публикаций:
- **PRISMA** - систематические обзоры и мета-анализы
- **CONSORT** - рандомизированные контролируемые испытания (РКИ)
- **STROBE** - обсервационные исследования
- **GENOMICS** - геномные исследования

### 📊 Результаты анализа (`reports/*/detailed_results.json`)
Детальные результаты проверки каждой статьи, включая:
- Тип исследования (автоматически определенный)
- Применённый чек-лист
- Степень соответствия (%)
- Детализация по каждому пункту чек-листа
- Найденные ключевые слова

## Преимущества JSON формата

### ✅ 1. Программная обработка
```python
# Легко загрузить и обработать
import json
with open('results.json') as f:
    data = json.load(f)
compliance = data['compliance_percentage']
```

### ✅ 2. Агрегация данных
Можно сравнивать результаты нескольких статей:
```bash
python compare_results.py
# → Автоматически анализирует все JSON файлы
```

### ✅ 3. Экспорт в разные форматы
- **CSV** для Excel: `comparison.csv`
- **HTML** дашборды: `dashboard.html`
- **Markdown** отчеты (уже реализовано)
- **PDF** (можно добавить)

### ✅ 4. Визуализация
```bash
python visualize_ascii.py
```

Создает:
- 📊 ASCII графики в терминале
- 🌐 Интерактивный HTML дашборд
- 📈 Детализацию по секциям
- 📑 Сравнительные таблицы

### ✅ 5. Универсальность
JSON читается в любом языке:
- Python, JavaScript, R, Julia
- Веб-приложения, API
- Базы данных (MongoDB, PostgreSQL JSON)

## Доступные инструменты визуализации

### 1️⃣ Сравнительный анализ
```bash
python compare_results.py
```

**Результат:**
- Таблица сравнения статей
- Агрегированная статистика
- Топ-5 часто отсутствующих элементов
- Экспорт в CSV

### 2️⃣ ASCII визуализация + HTML дашборд
```bash
python visualize_ascii.py
```

**Результат:**
- Цветные прогресс-бары в терминале
- Детализация по секциям для каждой статьи
- Сравнительная таблица
- **Интерактивный HTML дашборд** → `dashboard.html`

### 3️⃣ Существующие Markdown отчеты
```bash
python detailed_check.py article.pdf
```

**Результат:**
- `*_detailed_report.md` - читаемый отчет
- `*_detailed_results.json` - структурированные данные

## Примеры использования

### Сравнить все проанализированные статьи
```bash
python compare_results.py
```

Вывод:
```
╔════════════════════════════════════════════════════════════════╗
║         СРАВНИТЕЛЬНЫЙ АНАЛИЗ СТАТЕЙ (из JSON)                 ║
╚════════════════════════════════════════════════════════════════╝

Статья                                   Тип             Соответствие
───────────────────────────────────────────────────────────────────
hydroxychloroquine_covid19               RCT             59.5%    🟡
wakefield_1998                           case_series     68.2%    🟡

📊 Средняя степень соответствия: 63.8%
```

### Открыть интерактивный дашборд
```bash
python visualize_ascii.py
# Откройте dashboard.html в браузере
```

### Экспорт для дальнейшего анализа
```python
# Все данные в CSV для Excel/R/статистического анализа
import pandas as pd
df = pd.read_csv('comparison.csv')
print(df.describe())
```

## Альтернативы JSON

| Формат | Плюсы | Минусы | Когда использовать |
|--------|-------|--------|-------------------|
| **JSON** ✅ | Универсальный, структурированный, легко парсить | Многословный для больших данных | Везде (по умолчанию) |
| CSV | Простой, Excel-дружественный | Не поддерживает вложенные структуры | Простые таблицы |
| YAML | Читабелен человеком | Медленнее парсится | Конфиги |
| XML | Стандартизирован | Очень многословный | Legacy системы |
| SQLite | Быстрые запросы | Требует БД | Большие объемы |
| Только MD | Читабелен | Сложно обрабатывать программно | Только для чтения |

## Вывод

**JSON оптимален для этого проекта**, потому что:

1. ✅ Легко создавать визуализации и дашборды
2. ✅ Можно агрегировать результаты из нескольких статей
3. ✅ Просто экспортировать в CSV, HTML, Excel
4. ✅ Универсальная интеграция с любыми инструментами
5. ✅ Подходит для автоматизации (CI/CD, веб-сервисы)
6. ✅ Version control friendly (git diff работает хорошо)

**Без JSON** вам пришлось бы:
- Парсить Markdown вручную (сложно и ненадежно)
- Создавать отдельный формат (изобретение велосипеда)
- Терять структурированность данных
- Усложнить автоматизацию

## Следующие шаги

Вы можете расширить визуализацию:

1. **Добавить графики matplotlib/plotly**
   ```bash
   pip install matplotlib plotly
   python visualize_results.py
   ```

2. **Создать веб-приложение (Flask/Streamlit)**
   - Загрузка PDF → анализ → интерактивный дашборд

3. **Batch анализ**
   ```bash
   python batch_checker.py papers/*.pdf
   python visualize_ascii.py  # Сравнение всех
   ```

4. **Временной анализ**
   - Отслеживание качества публикаций по годам

5. **API для интеграции**
   - REST API для анализа статей
